// Native stub — Metro resolves PinPicker.web.tsx on web builds
export default function PinPicker(_props: { lat: number; lng: number; onChange: (lat: number, lng: number) => void }) {
  return null
}
